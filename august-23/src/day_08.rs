// https://leetcode.com/problems/search-in-rotated-sorted-array/

struct Solution;

impl Solution {
    pub fn search(nums: Vec<i32>, target: i32) -> i32 {
        let length: usize = nums.len();
        let mut current_index: usize = length / 2;
    }
}
